# Final Fixes — Agent 1: Metadata + CHANGELOG + Help

## Fixes
1. Fix CHANGELOG: rename agentproxy_* → novada_proxy_*, add Unreleased section for new features
2. Fix version: keep 1.8.1 everywhere (NO inflation), ensure package.json = config.ts = consistent
3. Fix --help to list all 10 tools
4. Tone down research "synthesis" → "findings_summary" or add clear note in description
