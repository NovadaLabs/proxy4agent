# Loop-2 Verification — LLM Schema Extract

**PASS with one minor gap (README).**

All five code-level fixes are confirmed: (1) prompt injection prevention is in place — `validateExtractParams` enforces `KEY_PATTERN = /^[a-zA-Z][a-zA-Z0-9_]*$/` with max 50-char keys and max 200-char values, rejecting empty schema objects and arrays above 20 entries (lines 432–467 of `src/tools/extract.ts`); (2) `extraction_prompt` contains only instructions + field list and explicitly tells the LLM to look at "data.content of this response" — no page content is embedded in the prompt (line 92); (3) content is truncated with `unicodeSafeTruncate(markdown, 50000)` not raw `.slice()` (line 83); (4) heuristic mode is fully intact — when `schema` is absent the code falls through to the heuristic branch and returns `mode: "heuristic"` (lines 124–157); (5) `inputSchema` in `src/index.ts` has `required: ["url"]` only, making `fields` optional, and the `schema` property is documented with its constraints (lines 131–145). Build is clean (`tsc` exits 0) and all 430 tests pass across 15 test files. The only outstanding gap is the README: it still describes `novada_proxy_extract` as heuristic-only and even says "Extraction uses heuristics, not LLM" in the troubleshooting table — schema/LLM mode is not mentioned anywhere in the docs. This is documentation debt, not a functional regression, but it should be updated before the next release.
